{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
 "start": "node_modules/.bin/json-server --host 0.0.0.0 --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "engines": {
    "node": "18.x"
  }
}
