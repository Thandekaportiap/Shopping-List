{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
 "start": "npx json-server --host 0.0.0.0 --watch db.json --port 3000"

  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "engines": {
    "node": "18.x"
  }
}
