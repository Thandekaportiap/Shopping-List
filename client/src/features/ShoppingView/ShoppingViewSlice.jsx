

import { createSlice } from '@reduxjs/toolkit'

const initialState = {
  value: 0,
}

export const shoppingViewSlice = createSlice({
  name: 'shoppingView',
  initialState,
  reducers: {
    
  },
})


export const {  } = shoppingViewSlice.actions

export default shoppingViewSlice.reducer